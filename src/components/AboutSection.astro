---
import { Shield, Users, Award, Clock } from '@lucide/astro';
---

<section class="py-24 px-4 bg-white">
  <div class="max-w-7xl mx-auto">
    <div class="text-center mb-16 opacity-0 animate-fade-in-up animate-duration-800">
      <h2 class="text-4xl font-bold text-gray-900 mb-4">Nuestra Experiencia en Gestión de Multas</h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Más de una década ayudando a conductores a resolver sus problemas de tránsito de manera eficiente y profesional.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {
        [
          {
            icon: Shield,
            title: "Experiencia Comprobada",
            description: "Más de 10 años especializándonos en la reducción de multas de tránsito con resultados garantizados."
          },
          {
            icon: Users,
            title: "Equipo Especializado",
            description: "Abogados expertos en derecho de tránsito que conocen cada detalle de la legislación vigente."
          },
          {
            icon: Award,
            title: "Resultados Exitosos",
            description: "95% de casos resueltos favorablemente, ahorrando dinero a nuestros clientes."
          },
          {
            icon: Clock,
            title: "Atención Inmediata",
            description: "Respuesta en menos de 24 horas y seguimiento personalizado de su caso."
          }
        ].map((feature, index) => (
          <div 
            class="p-6 bg-gray-50 rounded-lg shadow-sm hover:shadow-md transition-all duration-300 transform hover:-translate-y-1 opacity-0"
            style={`animation: fadeInUp 0.8s ease-out ${index * 0.1}s forwards`}
          >
            <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center mb-4 mx-auto">
              <feature.icon class="w-6 h-6 text-blue-600" />
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">{feature.title}</h3>
            <p class="text-gray-600">{feature.description}</p>
          </div>
        ))
      }
    </div>
  </div>

  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .animate-fade-in-up {
      animation: fadeInUp 0.8s ease-out forwards;
    }
  </style>
</section>